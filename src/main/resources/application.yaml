spring:
  application:
    name: YuutoAppoiments

  datasource:
    url: jdbc:postgresql://localhost:5433/yuutoappoiments
    username: postgres
    password: Elles2012
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  sql:
    init:
      mode: never

server:
  port: 8081

jwt:
  secret: 9a8c1f7e2b6d4c5f9a8c1fcartiefc5f9a8c1f7e2b6d4c5f
  expiration: 43200000 # 12 Horas
  refresh-expiration: 604800000  # 7 días